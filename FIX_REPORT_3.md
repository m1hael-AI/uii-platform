**Сделано!** 🚀

В админке AI News (вкладка "Расписание") добавил две новые кнопки:
1.  **🚀 Запустить сбор сейчас** — запускает `Harvester` немедленно (в фоне).
2.  **⚡ Запустить генерацию сейчас** — запускает `Generator` (в фоне).

**Как это работает:**
*   **Backend:** Созданы новые endpoints `/admin/news/run-harvester` и `/run-generator`. Они используют `BackgroundTasks` от FastAPI, чтобы не блокировать запрос.
*   **Frontend:** В интерфейс добавлены кнопки, которые вызывают эти endpoints.

Теперь можно запускать сбор новостей вручную в любое время.
